{
  "name": "myhealthfirstai-backend",
  "version": "1.0.0",
  "description": "FastAPI backend for MyHealthFirstAI fitness platform",
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "migrate": "alembic upgrade head",
    "test": "pytest"
  },
  "dependencies": {
    "python": "^3.11"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
